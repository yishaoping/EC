
你是CS架构领域的专家，我正在进行基于chipyard的开发，设计小核rocket校验大核boom的协同工作框架。请你分析我下面的内容，帮我梳理逻辑，整理语言，返回我一段可行的prompt，使得我可以交给agent以生成ppt。

纠错最重要的就是屏障和回滚。

屏障是为了防止对外部信息的改写扩散，从而造成无法挽回的结果。
回滚是对外部信息的改写进行修复。

所以关键问题就是分析core对外部信息进行了哪些改写？这样才能针对性地预防和修复。

第一个分类是从传播路径分层次进行分类。
第零层包括如下几部分：
从BOOM发出的，交给rocket校验的待检查部分。
boom发出的store、load、SC、LR、AMO，对L1D和外设进行读写。
boom对csr等寄存器的修改等。

我们重点关注的还是store、load、SC、LR、AMO这些部分。

第一层是boom 到L1D：
这一层分为是否缓存，可缓存的直接改写data array，不可缓存的写入mshr。

第二层是L1D到L2，BOOMIOMSHR到外设
缓存这边只能写回。
外设那边走总线。上了总线不好回滚了，这是屏障最外的部分，此处屏障可以与其他屏障分开。

第三层是L2到DRAM
只有缓存。


第二个分类是从修复难度进行分类。
第一等是不校验不能写出去的，如不可缓存的store和AMO。这些只能暂停等待校验，但是不一定要暂停boom。
第二等是额外信息修改的，如不可缓存的load，SC/LR对寄存器的修改等。这些可能需要备份，或者其他手段。
第三等是缓存系统的内容。这部分比较复杂，既涉及时间戳的更新，也涉及触碰屏障时的必需校验，还有回滚时对于原有数据的复原。
第四等是对于架构状态的复原。
第五等是无影响的。如可缓存的load等。

